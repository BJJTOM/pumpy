========================================
🚀 AWS 배포 - 지금 바로 시작!
========================================

📌 AWS 서버: 3.27.28.175

========================================
방법 1️⃣: AWS 콘솔 사용 (가장 쉬움!)
========================================

1. AWS 콘솔 접속
   → https://console.aws.amazon.com/

2. EC2 → 인스턴스 → pumpy 서버 선택

3. "연결" 버튼 클릭

4. "EC2 Instance Connect" 선택 → "연결"

5. 브라우저 터미널이 열리면 아래 명령어 실행:

========================================
📋 배포 명령어 (전체 복사해서 붙여넣기!)
========================================

cd /home/ubuntu/pumpy

# 백엔드 업데이트
cd gym_api
source venv/bin/activate
pip install -r requirements.txt
python manage.py makemigrations
python manage.py migrate
python manage.py collectstatic --noinput
sudo systemctl restart gunicorn

# 프론트엔드 업데이트
cd /home/ubuntu/pumpy/gym_web
npm install
npm run build
pm2 restart gym_web || pm2 start npm --name "gym_web" -- start
pm2 save

echo ""
echo "✅ 배포 완료!"
echo "🌐 http://3.27.28.175:3000"

========================================
✅ 배포 완료 후 확인
========================================

브라우저에서 접속:
→ http://3.27.28.175:3000

테스트:
✓ 대시보드 표시
✓ 회원 관리
✓ 회원 신청
✓ 상품 관리
✓ 매출 관리

========================================
🆘 문제가 생기면?
========================================

서비스 재시작:
sudo systemctl restart gunicorn
pm2 restart gym_web

로그 확인:
sudo journalctl -u gunicorn -n 30
pm2 logs gym_web

상태 확인:
sudo systemctl status gunicorn
pm2 status

========================================
📱 모바일 앱도 업데이트
========================================

웹뷰 URL을 다음으로 변경:
http://3.27.28.175:3000

========================================
🎉 완료!
========================================

웹:      http://3.27.28.175:3000
API:     http://3.27.28.175:8000/api
관리자:  http://3.27.28.175:8000/admin

========================================





