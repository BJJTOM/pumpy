━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 AWS 서버 빠른 시작 가이드 (5분!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

APK가 작동하려면 AWS 서버가 필요합니다!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1단계: AWS 콘솔 접속
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔗 https://console.aws.amazon.com/ec2

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2단계: 인스턴스 시작
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 왼쪽 메뉴 → "인스턴스" 클릭
2. IP가 "3.27.28.175"인 인스턴스 찾기
3. 상태 확인:

   ⚠️ "중지됨"이면:
      → 인스턴스 선택
      → "인스턴스 상태" → "인스턴스 시작"
      → 1-2분 대기

   ✅ "실행 중"이면:
      → 3단계로 이동

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3단계: Session Manager로 배포
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 인스턴스 선택
2. 상단 "연결" 버튼 클릭
3. "Session Manager" 탭 선택
4. "연결" 버튼 클릭
5. 터미널 창이 열리면 다음 명령어 복사 & 붙여넣기:

cd ~/pumpy && git pull && cd gym_api && source venv/bin/activate && pip install -r requirements.txt && python manage.py migrate && sudo systemctl restart gunicorn && cd ../gym_web && npm install && npm run build && pm2 restart gym-web && sudo systemctl restart nginx

6. 엔터키 → 5-8분 대기

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4단계: 완료 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

배포 완료 후:

1. 웹 브라우저에서 접속:
   🌐 http://3.27.28.175

2. APK 재실행:
   📱 앱이 정상 작동!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 문제 해결
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Session Manager가 안 보이면:
→ 인스턴스 재부팅 시도
→ 보안 그룹에서 포트 22, 80, 443 열기

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 완료!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

예상 시간: 5-10분
완료 후: APK 정상 작동 확인!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

